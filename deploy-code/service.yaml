apiVersion: v1
kind: Service
metadata:
  name: mylb
  labels:
    app: dev
spec:
  type: LoadBalancer
  selector:
    app: dev
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80